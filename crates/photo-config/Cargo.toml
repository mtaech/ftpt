[package]
name = "photo-config"
version.workspace = true
edition.workspace = true

[dependencies]
dirs.workspace = true
serde.workspace = true
thiserror.workspace = true
toml.workspace = true
# 可选：为 Tauri 端导出 TS 类型（tauri-specta）。默认构建不启用，行为零改动
specta = { workspace = true, optional = true }

[features]
specta = ["dep:specta"]

[dev-dependencies]
tempfile.workspace = true
