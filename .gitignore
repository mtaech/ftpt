/target
# Tauri 后端构建产物（workspace 成员，target 不在根目录）
crates/photo-tauri/src-tauri/target/
local-lib/
libraw.so
.worktrees/
node_modules/
dist/
.pi-subagents/
nul

# 识别资产（二进制，不入库）
models/
data/
tmp_smoke/

# 运行时日志（exe 同级 logs/ 目录）
logs/
