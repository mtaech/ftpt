# Photo Tool — 泛在语言（Ubiquitous Language）

> 本文档定义领域术语的精确含义。实现层（Rust Core）必须共用此术语表。
> 不含实现细节 — 配置文件、架构决策、API 签名见 `docs/adr/` 和各模块源码。

---

## 核心实体

### Capture（拍摄）

一个图片文件即一个 Capture。**扫描模型**下 JPG 与 RAW 各自独立成项（每文件一个 Capture，
不配对）；**显示层**按配置的堆叠模式分组（`photo-config StackMode`，设置弹窗可切换）：
- `ByTime`（默认，同组照片堆叠）：拍摄时间差 ≤2s 的连拍合并为一个堆叠卡片
- `ByFileName`（同文件名堆叠）：同 stem 文件合并（JPG/NEF 同画面）
- `None`：不堆叠，每文件一项

堆叠卡片显示主格式缩略图 + ×N 徽标（点击循环切换激活成员，预览工具条「格式 n/m」），
分组纯逻辑见 `photo-tauri/src/lib/stacks.ts`。

- 示例：`DSC_0001.jpg` → 一个 Capture；`DSC_0001.NEF` → 另一个 Capture；ByFileName 模式下两者合并为一个堆叠项（默认显示 JPG）

### SourceFile（源文件）


- 属性：路径、格式（ImageFormat）、文件大小
- 一个 Capture 恰好包含 1 个 SourceFile

### ImageFormat（图片格式）

文件所属于的图片格式分类。包括 7 种标准格式（JPEG, PNG, TIFF, HEIF, WebP, BMP, GIF）和 RAW 变体（44 种扩展名白名单）。

- **主显示文件**：所有 SourceFile 中 `display_priority` 最低的那个

### CaptureMeta（拍摄摘要）


- 包含：base_name, primary_path, primary_format, file_size, date_taken, extensions
- 设计目的：避免 UI 层加载完整文件路径列表（批量展示 5000+ 条目时节省内存和 FFI 开销）
- 堆叠分组键 = `base_name`（无扩展名）；`extensions` 供格式徽标/兄弟格式展示

---

## 标记


1-5 星评分，或 None（未评分）。持久化到文件夹数据库 `xmp_meta` 表。

5 种颜色标记：Red、Yellow、Green、Blue、Purple，或 None。持久化到 `xmp_meta` 表。

Pick（入选）或 Reject（淘汰）。持久化到 `xmp_meta` 表。

---

## 文件操作

### 画面（Shot）

同一目录下、文件名主干（stem）相同的一组 Capture，代表**同一次拍摄的不同格式记录**（如 `IMG_001.JPG` + `IMG_001.NEF`）。用户视角下它们是同一张照片（“兄弟文件也是同样的画面”）。

- 画面是**派生概念**：按 stem 对目录内文件分组得出，不是存储实体（scanner 仍按单文件生成 Capture）；网格**显示层**可配置堆叠（`ByFileName` 按画面合并 / `ByTime` 按连拍时间合并 / `None` 关闭，`stacks.ts`），批量文件操作可选择**画面粒度**（连同兄弟文件一起操作）或**单文件粒度**（只操作筛选出的格式）

### 兄弟文件（Sibling Files）

同一画面内的其他格式文件。`IMG_001.JPG` 的兄弟文件是 `IMG_001.NEF`、`IMG_001.PNG` 等。

### 批量文件操作（Batch File Operations）

对**当前筛选结果**（`display_order`）执行移动/复制/删除的动作集合，与筛选、标记贯通：

- **操作对象** = 当前筛选结果全量（纯筛选驱动），手动选择不参与批量范围
- **动作**：移动（到用户指定目录）/ 复制（到用户指定目录）/ 删除（回收站）
- **同步开关**：「同步同名文件」勾选后按 stem 将兄弟文件纳入操作集（默认关）；格式多选栏决定同步哪些格式（默认全选）
- **确认**：移动/复制选择目标目录即确认；删除强制二次确认

### 导入行为

- **Copy**：保留源文件，在目标位置创建副本
- **Move**：将源文件转移到目标位置

---

## 调整（Adjustment）

对一个 Capture 的可逆参数化修改集合。**母版文件永不修改**——调整以参数形式持久化，预览时实时应用，导出时才烘焙到像素（导出功能接线为后续阶段）。

- **per-capture**：每张图独立参数，随文件走（与评分/旗标/识别同模式持久化到文件夹数据目录）
- **全零参数 = 无调整**（与"从未识别"同构：没有调整记录即原图）
- v1 三类参数（裁切已移除，见 ADR 0007 修订）：

### 曝光（Exposure）

像素值变换。EV 标度，范围 ±2.0，步进 0.05；**+1.0 EV = 曝光量翻倍**。

### 对比度（Contrast）

像素值变换。范围 -100 ~ +100，0 中性。

### 饱和度（Saturation）

像素值变换。范围 -100 ~ +100，0 中性；-100 = 去饱和（黑白）。

### 调整视图（Adjustment View）

进入「调整」tab 时的预览上下文：**强制隐藏全部识别叠加层**（检测框、鸟眼角标、pending 识别框），预览显示调整后渲染。渲染层条件判断实现，不修改 bbox 显隐状态本身。

---

## 缩略图

### ThumbnailCache（缩略图缓存）

两级缓存：磁盘（JPEG 文件，hash 命名）+ 内存（UI 框架图片缓存）。

- 策略：先查缓存 → 未命中时生成 → 写入缓存 → 返回
- 生成策略：RAW 文件提取内嵌 JPEG 预览（不解拜耳），常规文件提取内嵌 EXIF 缩略图或完整解码

### 主预览

用户选中某张照片后，在预览面板中显示的大尺寸预览（区别于缩略图网格中的小缩略图）。通常按长边 2560px 渲染。

---

## 筛选与排序

### FilterCriteria（筛选条件）

组合式筛选器：文件名文本搜索、日期范围、评分下限、颜色标签、旗标、格式过滤。

### SortBy（排序方式）

FileName（文件名）、DateTaken（拍摄日期）、FileSize（文件大小）三种排序方式。

### SortDirection（排序方向）

Ascending（升序）或 Descending（降序）。

---

## 识别（Recognition）

### 识别（Recognition）

对一个 Capture 自动判定鸟种并评估鸟眼对焦的过程。四段流水线：**检测** → **分类** → **名录映射** → **鸟眼锐度**。

- 触发：用户对单张照片发起（批量识别为后续阶段）
- 输入源：主显示文件为 JPEG 等标准格式时直接全分辨率解码；RAW-only Capture 抽取 RAW 内嵌的最大预览图。用户看到的图 = 模型看到的图
- 结果持久化到**文件夹数据目录**（见下），不以 XMP 旁车形式产生伴生文件

### 检测（Detection）

在画面中定位鸟的位置，产出**检测框**。检测不到有效框时识别状态为 Unrecognized。

### 检测框（BBox）

鸟在画面中的矩形区域，归一化坐标 [x1, y1, x2, y2]（0–1，相对图像宽高）。

### 鸟种匹配（BirdMatch）

分类器输出的类别号经**名录库**映射后得到的具体鸟种。

- 属性：bird_id（名录库主键）、中文名、学名
- 类别号映射不到唯一鸟种时（无映射 / 多映射歧义）匹配失败，识别状态为 NeedsReview

### 鸟眼锐度分（Eye Sharpness Score）

对检测到的鸟眼区域计算的连续对焦锐度评分。第四阶段在检测框裁剪内运行眼检测，取最高置信度的单个眼框，多指标（拉普拉斯方差等）经 log 变换加权线性融合得出。

- 只保证**单调性**（分越高越锐利），不追求绝对刻度；「锐利/模糊」阈值在 UI 消费时才应用，不入库
- 无鸟 / 有鸟无眼 / 评分失败统一为**无分**（NULL），不影响 RecognitionStatus——Confirmed 只断言鸟种管线成功
- 持久化到文件夹数据目录 recognition 表（分 + 眼框），随文件走

### 眼框（Eye BBox）

评分所用的鸟眼矩形区域。复用 BBox 结构，**归一化坐标相对全图**（非相对鸟框），与检测框同一坐标系。

### 识别状态（RecognitionStatus）

一次识别结果的终态，三态：

- **Confirmed**：检测、分类、名录映射全部成功。UI 显示为「已识别」——它是**机器管线成功**的断言，不代表人工核实（当前无人工确认交互）
- **NeedsReview**：管线中途失败（分类异常 / 名录映射失败 / 源图不可用），需人工复核。UI 显示为「待复核」
- **Unrecognized**：检测阶段未发现鸟。UI 显示为「未检测到」

**"从未识别"不是状态——没有识别记录即为未识别。** 这区分了"没跑过"和"跑过但没鸟"。

### 名录库

鸟种参考数据：全分类体系的物种信息（中文名/学名/拼音/首字母）+ 分类器类别号到学名的映射表。只读，随应用分发。

照片文件夹内的应用私有数据目录，存放该文件夹的 SQLite 库（data.db）：EXIF **缓存表**（可从源文件重算，可删）、xmp_meta **真相表**（评分/色标/旗标，不可当缓存清）与 recognition **真相表**（识别结果，不可当缓存清）同居一库。

- 记录以主显示文件**相对文件夹根的路径**为键，文件夹整体搬移时库随目录走、记录全保持有效
- 文件操作（删除/移动/复制/重命名）时评分/旗标与识别记录均**随文件走**

---

## 配置

### AppConfig（应用配置）

持久化到 TOML 文件的应用配置。15 个字段：缩略图尺寸、主题、删除模式、导入行为、窗口尺寸/位置、缓存位置等。

- 配置文件路径：便携优先（exe 同目录），回退平台配置目录

### 便携模式

配置文件 `PT.toml` 与可执行文件在同一目录，允许 U 盘携带：应用 + 配置 + 缩略图缓存全部在可移动介质上。

---

## 数据流

```
文件系统 → Scanner → Vec<Capture>
                     │
        ┌────────────┼────────────┬───────────┐
        ▼            ▼            ▼           ▼
    Thumbnail     EXIF/导入    文件操作    Recognition
   (缩略图缓存)  (元数据+转换) (删除/移动/  (检测→分类→
        │            │       复制/重命名)   名录映射)
        │            │            │           │
        └────────────┼────────────┴───────────┘
                     ▼
              CaptureMeta → UI 层

   文件夹数据目录（.pt/data.db）：识别结果与评分/旗标写入/读取，
   EXIF 缓存表、xmp_meta 真相表与 recognition 真相表同居一库。
